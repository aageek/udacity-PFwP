import os
def rename_files():
    # (1) Get the file names from a folder
    file_list = os.listdir(r"/Users/Anand/GitHub/udacity-PFwP/prank")
    print (file_list)
    # (2) for each file, rename filename

rename_files()
